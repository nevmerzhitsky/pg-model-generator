"use strict"

/**
 * Plan:
 * 1. Create config of class names
 * 2. Write logic of creation of @type annotation as is
 * 3. Add ability to override config mapping
 * 4. Write logic of creation of @form annotation
 */

module.exports = module

function module() {
  return 100500
}
